using Windows.UI.Xaml.Controls;

namespace AiXiFan.Uwp.Controls
{
    public sealed class FloatingActionButton : Button
    {
        public FloatingActionButton()
        {
            DefaultStyleKey = typeof(FloatingActionButton);
        }
    }
}